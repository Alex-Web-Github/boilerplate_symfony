{% extends 'base.html.twig' %}

{% block title %}
  Marques
{% endblock %}

{% block body %}
  <div class="col-md-8 mx-auto">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Nom</th>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
        {% for marque in marques %}
          <tr>
            <td scope="row">{{ marque.id }}</td>
            <td scope="row">{{ marque.libelle }}</td>
            <td scope="row">
              <a href="{{ path('marque_show', { id: marque.id }) }}" class="btn btn-primary">Détails</a>
              <a href="{{ path('marque_edit', { id: marque.id }) }}" class="btn btn-primary">Modifier</a>
              <a href="{{ path('marque_delete', { id: marque.id }) }}" class="btn btn-primary">Supprimer</a>
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
    <a href="{{ path('marque_create') }}" class="btn btn-primary">Ajouter une Marque</a>
  </div>
{% endblock %}
